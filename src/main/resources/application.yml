# ????
server:
  port: 8080
  servlet:
    session:
      timeout: 21600

# ??mysql???
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/toefl?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 123456
    hikari:
      minimum-idle: 5
      # ?????????????600000?10???
      idle-timeout: 180000
      # ????????????10
      maximum-pool-size: 10
      # ?????????????????????,????true
      auto-commit: true
      # ?????
      pool-name: MyHikariCP
      # ??????????????????0???????????1800000?30??
      max-lifetime: 1800000
      # ?????????,??30???30000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  thymeleaf:
    encoding: UTF-8
    cache: false
    mode: HTML5
  # redis??
  redis:
    host: 101.132.251.198
    port: 6379
    password:

# ??MybatisPlus
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true